<nav class="nav-links">
    <figure><a href="{{ path('app_home') }}"><img class="logo-nav"
                                                  src="{{ asset('build/images/logo_white_fond_transparent_horizontal.png') }}"
                                                  alt="Logo de l'entreprise externatic"></a></figure>
    {% if app.user is defined and app.user is not null %}
        <div class="dropdown m-3">
            <button class="btn btn-light dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                {{ app.user.username }}
            </button>
            <ul class="dropdown-menu">
                <li><a class="dropdown-item" href="{{ path('app_profile') }}">Mon profil</a></li>
                <li><a class="dropdown-item" href="{{ path('app_appliedJobs') }}">Mes candidatures</a></li>
                <li><a class="dropdown-item" href="{{ path('app_favorite_jobs') }}">Mes favoris</a></li>
                <li><a class="dropdown-item" href="{{ path('app_logout') }}">Se déconnecter</a></li>
            </ul>
        </div>

    {% else %}

        <div class="sideBySide">
            <a class="no-connected" href="{{ path('app_login') }}">Candidat</a>
            <a class="no-connected" href="{{ path('app_login') }}">Entreprise</a>
        </div>

    {% endif %}

</nav>
